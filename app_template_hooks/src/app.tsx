import * as React from "react";
import * as ReactDOM from "react-dom";

// define an initial app state (if relevant)
const init = (container, initialState: AppState) => {
  ReactDOM.render(
    // Uncomment and import your root component
    // <RootComponent />,

    container
  );
};

// Replace nameofapp with a relevant name and uncomment below
// export const <nameofapp> = {
//   init: (initialState, domElement) => {
//     return init(domElement, initialState);
//   }
// };
